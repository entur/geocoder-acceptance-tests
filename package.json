{
  "name": "geocoder-acceptance-tests",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "local": "PELIAS_CONFIG=./pelias.json fuzzy-tester -e local -m 10",
    "dev": "PELIAS_CONFIG=./pelias.json fuzzy-tester -e dev -m 10",
    "tst": "PELIAS_CONFIG=./pelias.json fuzzy-tester -e tst -m 10",
    "prd": "PELIAS_CONFIG=./pelias.json fuzzy-tester -e prd -m 10",
    "v2local": "PELIAS_CONFIG=./pelias.json fuzzy-tester -e v2local -m 10",
    "v2dev": "PELIAS_CONFIG=./pelias.json fuzzy-tester -e v2dev -m 10",
    "v2tst": "PELIAS_CONFIG=./pelias.json fuzzy-tester -e v2tst -m 10",
    "v2prd": "PELIAS_CONFIG=./pelias.json fuzzy-tester -e v2prd -m 10"
  },
  "dependencies": {
    "pelias-fuzzy-tester": "^2.2.0"
  }
}
